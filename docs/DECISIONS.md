# NOVA — Decisions log

> Newest last. One short entry per decision. Only Claude or the Owner adds entries.

| # | Date | Decision | Why |
|---|---|---|---|
| D1 | 2026-09-22 | Name: NOVA (Networked Order & Value Analytics); products Orbit, Relay, Core, Atlas, Ledger, UI, Style | Owner choice; names in `brand.config.ts` only |
| D2 | 2026-09-22 | Broker: Zerodha Kite Connect (paid plan, one key in Phase 1) | Paid plan includes live + historical data |
| D3 | 2026-09-22 | Phase 1 = strategy building + backtesting only | Prove strategies before any real money |
| D4 | 2026-09-22 | UI first with static mocks (Stage A), backend after scope freeze (Stage B) | Get feedback on a visible workflow first |
| D5 | 2026-09-22 | Frontend: React + TS + Vite monorepo; backend: Python + FastAPI | Best libraries for each side |
| D6 | 2026-09-22 | Light microservices: core, broker, atlas, ledger, strategy, backtest | Enough separation without heavy integration cost |
| D7 | 2026-09-22 | UI library in the same monorepo; `ui-core` generic and publish-ready, `ui-trading` separate | Reuse in future projects |
| D8 | 2026-09-22 | Design system NOVA Style: dark navy default + light theme, IBM Plex Sans/Mono | Approved theme preview |
| D9 | 2026-09-22 | Strategies: visual builder and Python, both produce one Strategy Spec | One engine, one results format |
| D10 | 2026-09-22 | All segments designed from day one; engines built delivery → intraday → futures → options | Avoid redesign without multiplying bugs |
| D11 | 2026-09-22 | Finest data: 1-minute historical from Kite; ticks recorded live from go-live | Kite has no historical second/tick data |
| D12 | 2026-09-22 | Single super-admin in Phase 1; roles tables exist for later | Roles not finalised |
| D13 | 2026-09-22 | Mobile responsive from 360px | Family will use phones |
| D14 | 2026-09-22 | Claude plans and reviews (may fix during review); Gemini implements; task status is the lock | Parallel work without collisions |
| D15 | 2026-09-22 | Tailwind CSS v4 is configured in CSS (`@theme`) generated from `tokens.json`, with the default colours, radii and text sizes removed | Only token classes exist, so no hex values can creep in |
| D16 | 2026-09-22 | IBM Plex fonts are self-hosted via `@fontsource/*` (no font CDN) | Works offline and makes no third-party requests |
| D17 | 2026-09-22 | Contracts: money and prices in integer paise; timestamps as ISO-8601 UTC strings (`Z`); calendar dates as `YYYY-MM-DD`; strict objects | One unambiguous wire format for Stage B |
| D18 | 2026-09-22 | Storybook 10 lives in `ui-storybook`; stories sit next to their component (`X.stories.tsx`) in ui-core/ui-trading and are picked up by glob | Component, story and test stay together; one Storybook for both libraries |
| D19 | 2026-09-22 | ui-core components follow shadcn/ui patterns, written by hand (no CLI): `cva` variants; `cn` = clsx + tailwind-merge extended with the token text sizes; Radix only where behaviour needs it (Slot for `asChild`) | We own the source and nothing is regenerated; tailwind-merge must know the custom `text-*` sizes or it drops classes |
| D20 | 2026-09-23 | Form controls: native `<input>`/`<select>` (dates via `datetime-local`/`date`), Radix only for Checkbox and Switch. Controls take `label`/`error` props and never import react-hook-form or zod; apps wire RHF (`register`/`Controller`) with `@hookform/resolvers/zod`. Date values are UTC, shown in `Asia/Kolkata` via date-fns-tz | Native pickers work best on phones; ui-core stays props-only; no date-picker library |
| D21 | 2026-09-23 | Mock API: MSW handlers in `@nova/mocks` under `/api/v1` (any origin); bare JSON bodies (lists are arrays); errors are `ApiError` `{ error: { code, message } }`; `emptyHandlers`/`errorHandlers` for states | One wire format for mock and real; stories and tests can show empty/error states. Pagination envelope is decided at scope freeze |
| D22 | 2026-09-23 | `@nova/services` validates every response with its contract schema (`safeParse`; mismatch = `invalid_response` error). `VITE_DATA_MODE=mock` (default) fetches same-origin `/api/v1` served by MSW; `real` is refused until Stage B. TanStack Query hooks per endpoint; no retry on 4xx | Mock and real share one code path; bad data fails loudly at the boundary instead of deep in a screen |
| D23 | 2026-09-23 | Apps: `react-router` 7 (v8 needs React 19) with a shared `routes` array per app; mock sign-in accepts any non-empty username/password and keeps `{ userId, displayName }` in `sessionStorage` (no login endpoint or contract until Stage B); MSW browser worker starts from `main.tsx` only in mock mode via `@nova/mocks/browser` | Clickable prototype without inventing an auth API; the same routes serve the browser and memory-router tests |
| D24 | 2026-09-23 | Code editing uses CodeMirror 6 (`@codemirror/*` packages directly, no wrapper) inside ui-core `CodeEditor`, themed only with `var(--…)` tokens | Added to the stack for Python strategies (ARCHITECTURE: two authoring modes); small, accessible, works on phones; token theming keeps dark/light in sync |
| D25 | 2026-09-23 | Universe (symbols or index) moves from `StrategySpec` to `BacktestRun`; a strategy is rules only | Owner: pick symbols when backtesting; one strategy can be tested on many baskets |
| D26 | 2026-09-23 | Strategy list is a card grid fed by a `StrategyStats` summary endpoint (`GET /api/v1/strategies/stats`); screens never aggregate runs | Owner wants run counts and best/worst results per strategy; aggregation belongs to the backend |
| D27 | 2026-09-23 | Rate limits are rules per endpoint × window (second/minute/day) with `brokerLimit` (read-only, from broker docs) and `novaLimit` (editable, ≤ broker); daily windows carry `resetsAt` | Broker limits cannot be changed by us; a lower own limit keeps headroom and shows what is adjustable |
| D28 | 2026-09-23 | `BrokerProfile` contract holds API/plan details and useful links as data; API keys shown as last 4 characters only | Owner wants broker info and doc links in Relay; no secrets on the wire |
| D29 | 2026-09-23 | ui-core `DataTable` gains optional row selection (checkbox column) and a search box; pickers are built from it, not new list components | One accessible, mobile-ready list for symbol pickers and future bulk actions |
| D30 | 2026-09-23 | Checks: library packages have no build step (used from `src`); `tsc` runs only in `typecheck`; ESLint, Prettier and `tsc` cache under `node_modules/.cache` (tsbuildinfo git-ignored); Vitest uses the `threads` pool; `review:check` runs format, lint, typecheck, test, then build, one after another (parallel was slower on the owner PC: tests 309 s vs 103 s). While working, agents run only the checks for the packages they touch | The full check took about 6 min on Windows, mostly repeated or uncached work |
| D31 | 2026-09-23 | Scope freeze: Owner accepted Stage A round 2 with no changes. Screens, flows and contracts on `main` after NOVA-042 are the Phase 1 scope; changing them needs a decision entry and a task. Stage B starts | The backend is built against a fixed target |
| D32 | 2026-09-23 | Pagination (closes D21): lists that grow without bound (`/backtests`, `/backtests/{id}/trades`, `/data-jobs`, `/audit`) return `{ items, nextCursor }` (`nextCursor` opaque string or `null`) and take `?limit=` (1–200, default 50) and `?cursor=`. `/backtests` also takes `?strategyId=`. Bounded lists stay bare arrays (strategies, stats, accounts, rate limits, profiles, instruments = cash + F&O underlyings). Candles get a required date range with NOVA Atlas | Cursor pages stay stable while new rows arrive; small lists need no envelope |
| D33 | 2026-09-23 | Backend tooling: Python 3.12 runs only in Docker (host needs Docker Desktop, no Python). `backend/` is a uv workspace: `libs/*` (shared) and `services/*` (one package per service). FastAPI, Pydantic v2, pydantic-settings; SQLAlchemy 2 + Alembic from NOVA-047; ruff (lint + format), mypy `--strict`, pytest. Exact pins via `uv.lock`. One Compose file at the repo root; `docker compose run --rm backend-check` runs all backend checks | Same environment on the Owner's Windows PC and later the VPS; one command for checks |
| D34 | 2026-09-23 | Contract parity: Zod in `@nova/contracts` stays the source of truth. It emits JSON Schema per contract into `frontend/packages/contracts/schema/` (Vitest file snapshots, so stale files fail). Backend Pydantic models live in `backend/libs/nova_contracts`; its tests parse every mock JSON with the model and validate model output against the JSON Schema | One definition; both sides are checked against the same mocks and schema |
| D35 | 2026-09-23 | Kite access in Stage B: only the broker service calls Kite; API key and secret come from `.env` (git-ignored) only; access tokens are encrypted at rest; tests never call Kite (fake HTTP). No order-placement client code exists in Phase 1 | Keeps secrets and real-money paths out of everything except one audited service |
| D36 | 2026-09-24 | Dev machine (amends D33): Node via fnm (`.nvmrc`), pnpm only, Python via uv only (no pip, no system installs). Docker remains the backend gate, but host `uv run` in `backend/` (Python 3.12 from `.python-version`, same `uv.lock`) is allowed for fast package-scoped checks and editors. Compose services carry memory limits (stack ≤ 4 GB) inside the 12 GB WSL cap | Owner rebuilt the PC with fnm, pnpm, uv and a capped Docker Desktop (setup guide 2026-09-24); host checks are much faster than bind-mounted Docker runs on Windows |
| D37 | 2026-09-24 | Database (NOVA-047): lib `nova_db` holds SQLAlchemy 2 models (sync, psycopg 3) and hand-written Alembic migrations; `python -m nova_db upgrade` runs from a Compose `migrate` service before services start. Text ids `<prefix>_<sortable hex>`; money `bigint` paise; percents `numeric`; enums as text + CHECK; contract refinements as CHECKs. Run metrics are columns (stats aggregate in SQL); equity curve and per-symbol rows are JSONB. Runtime state (rate-limit usage, session status) is not stored in these tables. Each later task adds its own tables by migration. DB tests use a throwaway database inside `backend-check` | One schema owner; the DB enforces the same rules as the contracts; migrations stay reviewable |
| D38 | 2026-09-24 | Auth and gateway (NOVA-048): super-admin signs in with email + password (stdlib scrypt hash); a random session token lives in an HttpOnly SameSite=Lax cookie and only its SHA-256 is stored (`auth_sessions`, 12 h). NOVA Core owns sign-in, `/me` and the audit log, and forwards other `/api/v1` prefixes to services (URLs from env) after authenticating, with `X-Nova-User-*` headers and a shared internal token; services are not exposed to the host. New ApiError code `unauthorized` (401); `LoginRequest` contract. Test DB fixtures live in `nova_testing.db` | One place checks sessions; services trust only Core; no password or token is stored in clear |
| D39 | 2026-09-24 | Broker service (NOVA-049): `KiteClient` over httpx2 with an injectable transport (tests use a fake Kite); only session endpoints (login URL, token exchange, invalidate). Login state is an HMAC-signed `account.expiry` passed through Kite `redirect_params` (no storage); Kite `user_id` must equal the account's `client_id`. Access tokens are Fernet-encrypted (`cryptography`, key `NOVA_BROKER_TOKEN_KEY`) and expire at the next 06:00 IST; status is derived on read and `broker.session_expired` is audited once per expiry. Relay starts login by navigating to `GET /broker/accounts/{id}/login` (302 to Kite); the callback redirects back to Relay. Services check `X-Nova-Internal-Token` (`nova_common.internal`) | One audited place for Kite secrets; no forged callbacks; tokens useless if the database leaks |
| D40 | 2026-09-25 | Rate limiter (NOVA-050): one atomic Redis Lua call per Kite request checks every window of the endpoint (second/minute rolling logs, day counter per reset period); a denied request consumes nothing and counts as throttled; peaks per window are kept per day (`used`). Default NOVA limit = max(1, ⌊0.9 × broker⌋). Kite does not document when per-day limits reset: the reset time is a setting, `NOVA_KITE_DAILY_RESET` (IST, default 00:00), until Zerodha confirms it. Redis tests run in `backend-check` (db 15) | Correct under concurrency with no locks; tests control time; the unknown reset time is one setting to change, not code |
| D41 | 2026-09-25 | Jobs (closes the job-queue question): PostgreSQL is the queue. A worker claims the oldest `queued` row with `FOR UPDATE SKIP LOCKED`, sets `running`, and commits progress as it goes; one worker per job table in Phase 1, which requeues its `running` rows at start. Backtests (NOVA-055) use the same pattern on `backtest_runs`. Atlas reaches Kite only through broker endpoints under `/internal/…` (internal token, outside `/api/v1`, never forwarded by the gateway), each taking a limiter slot. The instrument universe (symbol, name, sector, indices) is a repo data file the Owner edits; Kite supplies tokens and F&O lot sizes | No new library or broker for jobs; job state and its row are one transaction; the Kite boundary of D35 stays intact; sector and index data need no second external API |
| D42 | 2026-09-25 | NOVA Ledger is a library (`libs/nova_ledger`) used in-process by the backtest engine, not an HTTP service; it becomes a service only when a screen or API needs it. Charge rates are rows in `charge_rates` (segment, effective date, Decimal strings), seeded from Zerodha's published schedule effective 2024-10-01; a rate change is a new row. Components round half-up to paise, STT to the rupee; GST is 18% of brokerage + exchange txn + SEBI fee + DP; the total is the sum of rounded components | Thousands of trades per run need no network hop; rates stay data, dated and auditable; the contract's total rule holds exactly |
| D43 | 2026-09-25 | Strategy writes (scope change for the editor's save): `POST /strategies` (`StrategyCreate` → `Strategy`, version 1, `draft`), `POST /strategies/{id}/versions` (`StrategyVersionCreate`; versions are immutable, latest + 1) and `PATCH /strategies/{id}` (`StrategyUpdate`: name, description, status). No delete in Phase 1 (runs reference versions). Mock handlers answer statelessly; screens switch in NOVA-059 | The editor needs somewhere to save; immutable versions keep every backtest reproducible |
| D44 | 2026-09-25 | Queueing a backtest (scope change for the run form): `POST /backtests` with `BacktestRunCreate` (strategy id + version, name, universe, period, capital, benchmark) → 201 `BacktestRun` `queued`, audited `backtest.run`. The backtest service runs from the Postgres queue like data jobs (D41; the queue helpers move to `nova_db.queue`). The service and worker (NOVA-055) are split from the engine (NOVA-062) | The run form needs somewhere to send runs; the queue, API and engine can be built and reviewed separately |
| D45 | 2026-09-25 | Backtest engine rules (v1): rules are evaluated at a bar's close and filled at the next bar's open (no look-ahead); stop-loss is checked before target inside a bar and fills at the stop or at the open on a gap; long only, one position per symbol, whole shares, shared cash; positions still open close at their last bar. Indicators run on rupee floats (signals only); every amount stays integer paise and charges come from NOVA Ledger. Numbers in rules are rupees for prices. Benchmark points are null until index candles exist | Conservative, reproducible results; the same rules extend to intraday (056) and Python strategies (057) |
| D46 | 2026-09-25 | Intraday backtests (NOVA-056) follow Zerodha MIS: from 15:20 IST positions close at that bar's open, pending entries are dropped and no new signals are taken; a position left open at a day change (missing late bars) closes at the previous bar's close. Intraday runs need an intraday timeframe and use `equity_intraday` charge rates | No overnight intraday positions, matching how the broker would square off |
| D47 | 2026-09-25 | Python strategies (NOVA-057): user code defines `class Strategy` with `on_bar(self, ctx)` returning "enter", "exit" or None. It is checked by AST (no imports, no `_` names or attributes, no eval/exec/open/getattr-style calls) and runs in a child process (`python -I`, empty environment, whitelisted builtins, CPU/memory/file limits, wall-clock timeout) that only exchanges JSON signals. The worker container is the outer boundary; no secrets are ever passed to the child | Owner's own code, but a mistake or pasted snippet must not reach the database, tokens or the network, or hang the worker |
| D48 | 2026-09-25 | Real mode (closes the Stage A refusal in D22): `vite --mode real` sets `VITE_DATA_MODE=real` and proxies `/api` to NOVA Core, so the browser stays same-origin and the HttpOnly cookie works without CORS or tokens in JavaScript. Sign-in posts `LoginRequest`; a small session hint in sessionStorage drives the UI and any 401 clears it. DemoBanner and demo wording appear only in mock mode. Relay starts the Kite login by navigating to the broker endpoint and shows the `?kite=` result | Mock and real share one code path; auth stays in the backend |
| D49 | 2026-09-25 | Ticks (NOVA-052): the recorder is a broker process (`python -m nova_broker record-ticks`, Compose profile `market`, never started by default) that reads Kite's WebSocket in `full` mode and batch-inserts into the `ticks` hypertable; it reconnects with backoff and stops at 15:30 IST. Atlas archives old ticks to Parquet (`date=/symbol=` folders, pyarrow) and deletes them only after the files are written. New dependencies: `websockets`, `pyarrow` | Second/tick data exists only if recorded live (D11); Kite traffic stays in the broker (D35); the database keeps a recent window, files keep history |
| D50 | 2026-09-25 | API docs (NOVA-063): `NOVA_API_DOCS` (default false) turns on each service's `/openapi.json` inside Compose and, on NOVA Core, `/api/v1/openapi.json` (Core merged with the forwarded services' paths) and Swagger UI at `/api/v1/docs`, without sign-in. Only a dev machine sets it; the VPS never does | One place to browse and try the whole API while developing, without exposing internal services or docs in production |
| D51 | 2026-09-25 | Indicator catalog (NOVA-064–068): one catalog of 38 indicators (name, label, group, params with default and integer flag) lives in `@nova/contracts` and is exported to `schema/indicators.json`; `nova_contracts.indicators` mirrors it (parity test). Integer params are ≥ 1, decimal params > 0. Write bodies (create strategy / add version) reject unknown or invalid params; read contracts stay tolerant so old specs still load, and the editor refills defaults on load. Price and indicator operands get optional `offset` (bars ago, 0–500, absent = 0). Levels (`prev_day_*`, pivots) use the previous IST day. Python mode gets the same indicators as `ctx.<name>(…)` with literal numbers only: the parent finds the calls by AST, computes the series with the engine's library and sends them to the child, which stays standard-library only | One definition drives the editor, validation, the engine and Python mode; fixes MACD silently ignoring `period`; the sandbox never imports NOVA code |
| D52 | 2026-09-25 | Broker accounts from Relay (NOVA-070): `POST /broker/accounts` (body `BrokerAccountCreate {label, clientId}`) shares `add_account` with the CLI (same checks, default rate-limit rules) and writes audit `broker.account_create` (migration 0005). The super-admin is the only user (D12), so no new permission. Editing, disabling and removing accounts stay out of scope; `create-admin` stays CLI-only (no one can sign in before it exists) | A rare but routine admin job no longer needs Docker; one code path keeps the screen and the CLI consistent |
| D53 | 2026-09-25 | Cost averaging (NOVA-069, 071; Owner choices 2026-09-25): optional spec field `averaging {dropPercent, maxAdds}` (absent = off, never written when off). While holding, the engine buys again each time the price falls `dropPercent` below the **last** buy, up to `maxAdds` extra buys per position, each sized by the strategy's normal sizing. Each add is a resting buy at the trigger: filled at min(open, trigger) when the low reaches it; a gap can fill several at the open. Stop-loss and target follow the **average** buy price; if one candle reaches both an add trigger and the stop, whichever is nearer the average fills first. The position stays **one trade**: qty = all shares, entry time = first buy, entry price = average (rounded), gross = exit value − exact total cost; charges are computed once on the whole trade. Works for visual and Python strategies | Owner picked the simplest rules; one trade keeps metrics, charges and the results screens unchanged. Changes D45's one-fixed-position rule |
| D54 | 2026-09-25 | Relay is the control panel (NOVA-072–078; Owner choices 2026-09-25): every routine admin action has an endpoint and a Relay screen, audited with the signed-in user; the CLI commands stay and share the same functions. Docker/CLI remain only for setup: `create-admin`, `new-token-key`, migrations, starting/stopping the stack. Relay gets **no** Docker access. (1) Downloads are queued and queued/running jobs cancelled from Relay. (2) The universe moves from `universe.csv` to a `universe` table (seeded from the file once; the file is then removed), edited in Relay; sync runs from Relay. Changes D41's "repo data file". (3) The tick recorder runs always (no Compose profile) and sleeps; a `recorder_settings` row (on/off + symbols) set in Relay decides whether it records 09:15–15:30 IST on weekdays; each session is a `tick_record` data job. Changes D49's "never started by default". (4) Tick archiving is an `archive` data job run by the Atlas worker. Adding a *different broker* (not Zerodha) is a new integration, not part of this | The Owner runs NOVA from Relay; Docker commands for daily work are error-prone. Settings in the database need no image rebuild |
| D55 | 2026-09-26 | Kite keys in the database, secret sealed by a passphrase (NOVA-079–083; Owner choices 2026-09-26). (1) Each broker account has its own Kite app (each family member owns one; Phase 1 builds for the Owner's account only). A `broker_kite_apps` row per account holds the API key (plain: it is public in the login URL and needed all day), the API secret sealed with the Owner's **passphrase** (scrypt n=2¹⁵ r=8 p=1 → AES-GCM, random salt + nonce, account id as associated data; standard library + `cryptography`, no new dependency), and the app details (plan, renewal, postback URL, static IP). The redirect URL is derived from `NOVA_RELAY_URL`. The passphrase is never stored or logged. Forgotten passphrase = enter key and secret again. (2) Login: Relay → Kite → callback, which keeps the request token in Redis (2 min, one use) and returns to Relay; Relay asks the passphrase; `POST …/login/finish` opens the secret, exchanges the token and forgets both. 5 wrong tries drop the pending login. (3) Access tokens stay sealed with `NOVA_BROKER_TOKEN_KEY` (D39) so the recorder and jobs run unattended. (4) `.env` keeps only infrastructure: database, Redis, ports, internal token, token key, Relay URL, cookie flag, API docs, the Kite daily reset (pending, D40). `NOVA_KITE_*` app values are removed. (5) CLI keeps only setup and process commands: `create-admin`, `new-token-key`, migrations, `worker`, `recorder`, the backtest worker. Changes D39 (keys from env, single Kite client), D52 and D54 ("CLI commands stay"). (6) Relay's broker screens become two: **Broker** (profile, accounts, limit warnings) and the account page (session, Kite app, rate limits) | Everything routine is done in Relay; a leaked database backup, even with `.env`, does not reveal the API secret; one passphrase prompt a day |
| D56 | 2026-09-26 | Whole NSE stock list, automatic indices, daily sync (NOVA-084–089; Owner choices 2026-09-26). (1) Sync adds **every NSE equity** in Kite's instrument list to `universe` (segment `NSE`, type `EQ`; plain symbols and the `-BE`/`-BZ`/`-SM`/`-ST` series; other suffixes such as bonds and SGBs are skipped). Name from Kite; sector = NSE "Industry" from the index files, else `Unclassified`. Hand-added rows stay; symbols Kite no longer lists are reported as missing, never deleted. (2) A symbol that a sync adds when an earlier sync has already completed is a **new listing** (`universe.new_listing`, cleared by the Owner). This is how IPOs show up: Kite has no IPO calendar, a stock appears in its list on the listing day. (3) Indices are rows of a `market_indices` table (seeded list: display name, Kite trading symbol, NSE constituent file). Membership comes from NSE's public constituent CSVs, fetched by the **broker service** (the only service that calls outside APIs, D35); index tokens come from Kite's `INDICES` segment. `IndexName` becomes a string checked against that table on write. A file that cannot be fetched keeps that index's old membership and is named in the job result. (4) Sync is an `instrument_sync` data job run by the Atlas worker: `POST /market-data/instruments/sync` queues it (202, returns the job). The worker also queues one each weekday at or after 08:45 IST, once a Kite session exists, if none completed or is queued/running today. (5) Candle writes are batched (Postgres takes ≤ 65,535 values per statement); every failed job says what happened and what to do; job screens refresh while a job is queued or running | The Owner wants all stocks, more indices and new listings without typing them in. Kite supplies symbols and tokens, NSE supplies membership, the D35 boundary stays. A job shows progress and a clear failure reason. Changes D41 ("sector and index data need no second external API") and D54 (2) |
| D57 | 2026-09-26 | Realtime updates and planned, resumable downloads (NOVA-090–094; Owner choices 2026-09-26). (1) **WebSocket** `GET /api/v1/ws` on NOVA Core, signed in with the session cookie (same check as HTTP). Server → client messages `{type, data}`; first type `data_job.updated` (full `DataJob`). Source: a Postgres trigger on `data_jobs` runs `pg_notify('nova_events', …)` on insert/update; Core `LISTEN`s and fans out, so every job type (downloads, sync, archive, ticks) is covered with no service code. Live prices and orders later add Redis pub/sub as a second source into the same socket. Screens fall back to polling (NOVA-084) while the socket is down. (2) A download is **planned before it runs**: `POST …/jobs/plan` creates a `draft` job with its steps (stock × Kite-sized date chunk) and a plan: steps already covered by stored candles, requests, estimated rows and size, estimated duration and start (after the queue ahead). The Owner picks **Skip data already there** (default) or **Overwrite**, then **Start** (`draft` → `queued`). Drafts not started expire after 24 h. (3) **One download at a time** (D41 unchanged). (4) Steps are saved as they finish: **Pause** (worker stops after the current step), **Resume**, and a worker restart continue from the next unfinished step. (5) Market hours (weekdays 09:15–15:30 IST): a setting, **Slow down** (default: at most 1 historical request/s) or **Full pace** (2/s, the account limit rule); manual Pause always works. (6) New download and the recorder's stock picker can add stocks in bulk by index or sector | The Owner wants to see cost and time before a big job starts, never re-download what exists, and survive a laptop sleep or restart. NOTIFY needs no new dependency. Kite's order limit is a separate bucket, so this protects the session and connection, not the order budget |
| D58 | 2026-09-26 | Scale for years of 1m data, and backtest progress (NOVA-099–102; Owner request 2026-09-26: 1,000 stocks × 5 years of 1m, 500 stocks of live ticks). (1) `candles` is TimescaleDB-compressed after 30 days (segments exchange, symbol, timeframe; policy every 6 h). (2) Downloads are `1m` or `1d` only; `3m 5m 15m 30m 1h` are rolled up from 1m at read time, buckets from 09:15 IST; `1d` always comes from Kite (official close); stored 3m–1h rows are ignored. (3) Backtest runs report progress (stage `loading`/`signals`/`simulating`/`saving`/`done`, counts, date reached, trades so far, percent), written at most once a second from a separate session; Orbit polls every 2 s while a run is queued or running | ~465 M 1m rows are ~80 GB plain; one 1m download serves every intraday timeframe and they always agree; long intraday runs need visible progress |
| D59 | 2026-09-26 | Backtest worker survives crashes (NOVA-103; live OOM 2026-09-26). (1) On start the worker marks runs left `running` as `failed` with a plain "worker stopped" message; it no longer requeues them. (2) `backtest-worker` has `restart: unless-stopped` and `mem_limit: 1536m`. (3) A run loads bars one stock at a time and fails early once it passes `NOVA_BACKTEST_MAX_BARS` (default 1,500,000, tuned by measurement). (4) Whole-stack memory rule becomes ≤ 7 GB (was 4 GB; already 5.1 GB after NOVA-098). A lower-memory engine is a later task | A requeued OOM run kills the worker again on every restart; one plain failure beats a run stuck in `running` forever |

## Pending
- Options historical data vendor (needed before the options engine, after NOVA-059).
- Kite daily reset time for per-day limits — a setting since NOVA-050 (default 00:00 IST, D40); Owner to confirm with Zerodha support.
- Family roles and permissions — after Phase 1 (D12 keeps a single super-admin).
- Domain and trademark check for NOVA naming (Owner; not blocking).
