[project]
name = "nova-testing"
version = "0.0.1"
description = "Test helpers shared by NOVA packages (contract parity kit, D34)"
requires-python = "==3.12.*"
dependencies = [
    "jsonschema==4.26.0",
]

[build-system]
requires = ["uv_build==0.12.18"]
build-backend = "uv_build"
