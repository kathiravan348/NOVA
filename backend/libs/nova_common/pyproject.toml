[project]
name = "nova-common"
version = "0.0.1"
description = "Shared settings and error handling"
requires-python = "==3.12.*"
dependencies = [
    "fastapi==0.141.1",
    "nova-contracts",
    "pydantic-settings==2.15.0",
]

[build-system]
requires = ["uv_build==0.12.18"]
build-backend = "uv_build"

[tool.uv.sources]
nova-contracts = { workspace = true }
